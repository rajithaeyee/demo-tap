{
  "name": "demo-tap",
  "version": "1.0.0",
  "description": "Chrome extension that shows an elegant click ripple for demos and recordings.",
  "private": true,
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "typescript": "^5.4.0"
  }
}
